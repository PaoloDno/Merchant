const Product = require('../models/productModel');
const { body, validationResult } = require('express-validator');

const createProduct = [
  async (req, res, next) => {
  try {
    const { basicInfo , categoryDetails, specification } = req.body;






  } catch(error){

  }
  }
]